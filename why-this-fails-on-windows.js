console.log("Wait, if it's maximized: false, that means on Windows, `window.maximize()` inside `resizeWindowToPlayer` didn't stick or didn't work? Or the snapshot was taken too early? But it's in a poll, so it waited 5s and it was still false. Why would `win.maximize()` not result in `isMaximized() === true` on Windows? The electron-test.js we ran locally successfully printed 'Maximized' except `isMaximized()` printed `false` after `maximize()`? Wait, let's look at the result of max-test.js from earlier.");
